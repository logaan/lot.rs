#!/usr/bin/env bash
# Run clippy with warnings treated as errors. Part of the CI / pre-commit gate
# (see scripts/check).
set -euo pipefail
cd "$(dirname "$0")/.."
exec cargo clippy --all-targets -- -D warnings
