#!/usr/bin/env bash
# Check formatting without modifying files. Part of the CI / pre-commit gate
# (see scripts/check). Use scripts/lint-autofix to actually reformat.
set -euo pipefail
cd "$(dirname "$0")/.."
exec cargo fmt --all --check
