- The new-Thing form now makes **Create** the primary button (was **Create and
  send**), and every button shows an underlined ctrl-letter that is its own
  shortcut — Cance**l** (`ctrl+l`), C**r**eate (`ctrl+r`), Crea**t**e and send
  (`ctrl+t`). Plain Create moved from `ctrl+s` to `ctrl+r` so the underline
  lands on a letter in the word; `escape` still cancels.
