- New `lot-coordinate-begin` skill (*Update plan and begin coordination*):
  folds the human's answered Decisions into the draft Steps as appended
  updates — logged on the root before any execution — then executes the
  reconciled plan, monitoring workers with `lot watch --thing` rather than
  polling. Installed by `lot claude install`; reached by `lot claude send`-ing
  the artifact Thing a decide coordinator authors.
