- The Textual UI now asks before discarding a modal form you have typed into.
  Cancelling a New Thing / New Update / generic command form (via `escape` or
  the Cancel button) that holds content you have entered raises a "Discard this
  form?" confirmation; an untouched form — including one that only carries a
  prefilled id — still closes immediately with no prompt.
